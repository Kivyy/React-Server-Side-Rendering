import React from 'react';

const Home = () => {
  return (
    <div>
      <div>I am testing the new nodemon and webpack </div>
      <button onClick={() => console.log('hi there')}>Press me </button>
    </div>
  );
};

export default Home;
